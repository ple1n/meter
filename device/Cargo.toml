# This file was automatically generated.
[workspace]

[package]
edition = "2021"
name = "stm32f1"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = [
    "inline-asm",
    "critical-section-single-core",
] }
cortex-m-rt = "0.7.3"
defmt = { version = "0.3.8", optional = true }
defmt-rtt = { version = "0.4.1", optional = true }
embassy-executor = { path = "../../embassy/embassy-executor/", features = [
    "arch-cortex-m",    
    "executor-thread",
    "integrated-timers",
    "nightly",
] }
embassy-stm32 = { path = "../../embassy/embassy-stm32/", features = [
    "defmt",
    "stm32f103c6",
    "unstable-pac",
    "memory-x",
    "time-driver-any",
] }
embassy-sync = { path = "../../embassy/embassy-sync/", version = "0.6.0" }
embassy-time = { path = "../../embassy/embassy-time/", features = [
    "tick-hz-32_768",
] }
panic-halt = "0.2.0"
panic-probe = { version = "0.3.2", features = ["print-defmt"], optional = true }
embassy-futures = "*"
embassy-usb = { path = "../../embassy/embassy-usb/", version = "0.3.0" }
static_cell = "2.1.0"

ppproto = "0.2.1"
serde = { version = "1.0.215", default-features = false, features = ["derive"] }

postcard = "1.0.10"
heapless = { version = "0.8.0", features = ["serde"] }
common = { version = "0.1.0", path = "../common" }

[[bin]]
name = "stm32f1"
test = false
bench = false

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true
features = ["defmt"]

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = [
    "dep:defmt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
    "embassy-usb/defmt",
    "postcard/defmt",
    "heapless/defmt-03",
    "ppproto/defmt"
]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = ["defmt", "defmt-rtt"]
